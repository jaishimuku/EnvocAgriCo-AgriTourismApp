class Farmfield {
 constructor   (id,
    myfarmFieldId,
    title,
    sprice,
    mprice,
    lprice,
    description,
    src) 
    {
        (this.id = id),
        (this.myfarmFieldId = myfarmFieldId),
        (this.title = title),
        (this.sprice = sprice),
        (this.mprice = mprice),
        (this.lprice = lprice),
        (this.description = description),
        (this.src = src);
    }
}
export default Farmfield;